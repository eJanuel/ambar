import { VoidBlock } from "../../types/Map.types";
import { VOID_TYPES_ENUM } from "../../types/enums/Cells.enum";

const AIR: VoidBlock = {
    type: VOID_TYPES_ENUM.AIR,
};

export const VOID = {
    AIR,
}